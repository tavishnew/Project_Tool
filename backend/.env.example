# Example DATABASE_URL for a Postgres-compatible provider (replace with real credentials)
# For Neon, use the connection string provided by your Neon project.
DATABASE_URL=postgresql://user:password@host:5432/database_name
